# Node / pnpm / monorepo
node_modules/
dist/
build/
coverage/
*.tsbuildinfo
.turbo/
.pnpm-store/

# Env & secrets — никогда не коммитим (см. docs/17_CONFIGURATION.md)
.env
.env.*
!.env.example

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/

# Logs & local
*.log
logs/
tmp/
demo/.pgdata/
uploads/
desktop.ini

# Дампы БД и архивы uploads от update.sh/BackupService — персональные данные
# диалогов; случайный git add -A не должен их закоммитить (реаудит RA-I-10)
backups/
*.dump
*uploads*.tar.gz
