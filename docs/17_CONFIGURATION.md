---
id: DOC-017
title: Конфигурация
project: Universal Chat — Self-Hosted AI Chat Platform
version: 0.1.0
status: draft
audience: admin, developer
priority: medium
summary: Все параметры конфигурации: переменные окружения .env (пример файла), функциональные настройки в админке (AI-провайдер, SMTP, бэкапы), шифрование секретов и ротация APP_SECRET, приоритеты значений.
when_to_read: При установке, изменении настроек, отладке конфигурации.
when_not_to_read: При разработке бизнес-логики.
keywords: конфигурация, env, переменные окружения, app_secret, smtp, провайдер, бэкапы, шифрование секретов, ротация
related:
  - DOC-016
  - DOC-019
  - DOC-015
---

# Конфигурация

## Краткое содержание

- Два уровня конфигурации: `.env` и настройки в БД (админка).
- Переменные окружения (таблица + пример).
- Настройки админки.
- Шифрование секретов и ротация ключа.
- Приоритеты.

## 1. Два уровня конфигурации

| Уровень | Что живёт | Кто меняет | Когда читается |
|---|---|---|---|
| `.env` на сервере | Инфраструктура: секреты окружения, подключения, режим | installer / сисадмин | старт контейнеров |
| `settings` в БД (через админку) | Функциональное: AI-провайдер, SMTP, бэкапы, телеметрия | администратор | runtime, без рестарта |

Принцип: всё, что можно менять из UI, — в БД; в `.env` только то, что нужно до старта приложения.

## 2. Переменные окружения

| Переменная | Обязательна | Назначение | Дефолт |
|---|---|---|---|
| `APP_SECRET` | да* | Ключ шифрования секретов в БД (AES-256-GCM); генерирует installer. *Без него api стартует, но шифрование настроек недоступно | — |
| `DATABASE_URL` | нет | Подключение PostgreSQL; без неё — деградация health (Фаза 0) | — |
| `REDIS_URL` | нет | Redis: fanout Socket.IO при >1 инстансе; без неё in-memory adapter (MVP-профиль) | — |
| `NODE_ENV` | — | production/development | development |
| `PORT` | — | Порт api внутри контейнера | 3000 |
| `TRUST_PROXY` | — | Число доверенных прокси-хопов (req.ip из X-Forwarded-For). За Caddy = 1; не задан — прямое подключение | — |
| `LOG_LEVEL` | — | Уровень логов (DOC-019) | info |
| `SETUP_TOKEN` | авто | Одноразовый токен визарда (печатает installer) | — |
| `BACKUP_DIR` | — | Куда worker складывает дампы и маркер last-backup.json | ./backups |
| `UPLOAD_DIR` | — | Каталог загруженных файлов знаний (архивируется в бэкап); volume /app/uploads | ./uploads |
| `BACKUP_AT` | — | Локальное время ночного бэкапа HH:MM | 03:00 |
| `ALERT_EMAIL` | — | Email алерта при ошибке бэкапа (не задан — SMTP-алерты выключены) | — |
| `APP_VERSION` | — | Версия сборки (health, диагностика) | 0.1.0 |
| `ADMIN_STATIC_DIR` | — | Каталог собранной SPA-админки: задан в образе (`/app/admin-static`) → api раздаёт `/admin` со SPA-fallback; не задан (dev) — админку отдаёт Vite :5173 (реаудит RA-I-2) | — |
| `WIDGET_JS_PATH` | — | Путь к бандлу виджета, который отдаёт `GET /widget.js` (в образе `/app/apps/widget/dist/widget.js`); вне схемы env — читается контроллером напрямую (реаудит RA-I-13) | `apps/widget/dist/widget.js` |
| `BACKUP_S3_ENDPOINT/_KEY/_SECRET/_BUCKET` | — | Удалённый бэкап (опция; backlog D-16) | — |

> Удалено из схемы по итогам аудита IR-059: `APP_URL` и `TRUST_PROXY`-«обязательна» не
> соответствовали коду; фактическая схема — `apps/api/src/config/env.ts`.

Пример `.env`:

```text
APP_SECRET=9f2c8a...            # сгенерирован installer; хранить у заказчика
DATABASE_URL=postgres://unichat:S3cret@postgres:5432/unichat
REDIS_URL=redis://redis:6379/0
NODE_ENV=production
PORT=3000
TRUST_PROXY=1                   # api за Caddy: req.ip из X-Forwarded-For
UPLOAD_DIR=/app/uploads
BACKUP_DIR=/app/backups
BACKUP_AT=03:00
LOG_LEVEL=info
# опционально — удалённые бэкапы:
# BACKUP_S3_ENDPOINT=https://s3.example.com
# BACKUP_S3_KEY=...
# BACKUP_S3_SECRET=...
# BACKUP_S3_BUCKET=unichat-backups
```

## 3. Настройки в админке (`settings`)

| Блок | Параметры |
|---|---|
| AI-провайдер | Base URL, API-ключ (шифруется), chat-модель, embedding-модель, temperature, max tokens, timeout; пресет «Local (Ollama)» |
| SMTP | host, port, user, пароль (шифруется), from — для email-уведомлений операторам |
| Бэкапы | расписание, retention (дни/недели), S3-опция |
| Телеметрия | opt-in отправка ошибок (по умолчанию выключена) |

> Лимит документов на проект и размер файла в настройках (аудит IR-059) не
> реализованы: файл ≤ 25 МБ зашит в код (`knowledge.controller`); лимиты на проект
> — backlog (D-16).

Смена AI-провайдера/моделей — DOC-011; смена embedding-модели запускает переиндексацию (DOC-012 §5).

## 4. Шифрование секретов

- Секретные значения (`API-ключ`, `SMTP-пароль`) шифруются AES-256-GCM перед записью в `settings`; ключ — `APP_SECRET` из `.env`.
- В API/UI секреты маскируются (`sk-...ab12`); повторная запись — новое значение.
- `.env` в бэкапы СУБД не входит; заказчик хранит `APP_SECRET` в своём менеджере паролей.

## 5. Ротация APP_SECRET

1. Ввести новый `APP_SECRET_NEW` рядом со старым → рестарт.
2. Служебная команда re-encrypt: все секреты БД расшифровываются старым и шифруются новым.
3. Удалить старую переменную → рестарт. (Процедура уточняется при реализации — TBD.)

## 6. Приоритеты значений

`значение из запроса/настройки проекта` → `настройки установки (settings)` → `.env` → дефолт кода. При конфликте приоритет у более специфичного уровня.

## Частые ошибки

- **`APP_SECRET` в бэкапе/репозитории** — только `.env` на сервере и менеджер паролей заказчика.
- **Смена `DATABASE_URL` без миграции** — новая пустая БД выглядит как «потеря данных»; восстановить из бэкапа (DOC-020).
- **`TRUST_PROXY=0` за прокси** — rate limit считает все запросы одним IP.
- **Правка `.env` без `docker compose up -d`** — переменные читаются на старте контейнеров.

## Связанные разделы

- Установка — DOC-016
- Логи и диагностика — DOC-019
- Шифрование и угрозы — DOC-015
